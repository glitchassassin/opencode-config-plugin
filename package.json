{
  "name": "opencode-config-plugin",
  "version": "1.0.1",
  "description": "OpenCode tool to safely update configuration with JSON schema validation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.2.24",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "keywords": [
    "opencode",
    "plugin",
    "config",
    "validator"
  ],
  "license": "MIT"
}
